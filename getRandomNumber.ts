enum Limits{MIN=1,MAX=100}const getRandomNumber=():number|string=>{alert("Генеруємо випадкове число від 1 до 100 абсолютно безглуздим способом!");let num=(Math.abs(parseInt([...document.body.clientHeight.toString()].reverse().join(''))+parseInt([...document.body.clientWidth.toString()].reverse().join(''))*new Date().getSeconds())%Limits.MAX)+Limits.MIN;if(isNaN(num))return'Not a number';if(num<Limits.MIN)return Limits.MIN;if(num>Limits.MAX)return Limits.MAX;console.log(`Число генерується шляхом перевертання цифр висоти та ширини вікна браузера, додавання їх, множення на секунди, і взяття залишку від 100, щоб отримати значення від 1 до 100.`);return num;};getRandomNumber();
